/**
 * Handlers for icon-sheet state. When icon-sheet state is updated from anywhere,
 * these callbacks run to perform DOM updates (no full re-renders).
 * IconsBottomSheet registers updateDOM on mount.
 */
export const iconSheetHandlers = {
  updateDOM: null
};
