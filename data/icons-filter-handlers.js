/**
 * Handlers for icons-filter state. When icons-filter state is updated from anywhere,
 * these callbacks run to perform DOM updates (no full re-renders).
 * SwDocsSwitchIconsScreen registers updateGrid on mount.
 */
export const iconsFilterHandlers = {
  updateGrid: null
};
